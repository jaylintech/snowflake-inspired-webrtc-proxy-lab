linters:
  enable:
    - gofmt
    - goimports
    - govet
    - staticcheck
    - gosimple
    - unused
    - ineffassign
    - errcheck

linters-settings:
  goimports:
    local-prefixes: snowflake-inspired-webrtc-proxy-lab

run:
  timeout: 3m
